import React from 'react';
import BackButton from '../../BackButton';

function Accordion() {
    return (
        <div>
            Accordion
            <BackButton />
        </div>
    );
}

export default Accordion;
